---
- name: "[template] Build /etc/docker/daemon.json from template"
  become: true
  template:
    src: daemon.j2.json
    dest: /etc/docker/daemon.json
    mode: 0600
  notify: "[systemd] Reload Docker"
