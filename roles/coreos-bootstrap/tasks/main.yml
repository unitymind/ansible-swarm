---
- name: Check is bootstrapped
  raw: stat $HOME/.bootstrapped
  register: bootstrapped
  changed_when: false
  ignore_errors: true

- name: "Run {{ bootstrap_type }}.sh"
  script: "{{ bootstrap_type }}.sh"
  when: bootstrapped | failed
